import asyncio

async def pull_images(image_list):
    """
    Copies or downloads the images from microscope storage to local project dirs.
    Stub implementation for now.
    """
    await asyncio.sleep(1)
    return image_list
